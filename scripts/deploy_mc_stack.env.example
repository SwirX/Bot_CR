# Copy to deploy_mc_stack.env (same directory) and fill in. All optional:
# unset values fall back to the defaults in deploy_mc_stack.sh.

# ── bot half ──
# BOT_REPO=/home/swirx/dev/robotics-cmc/Bot_CR
# BOT_PYTHON=/home/swirx/dev/robotics-cmc/Bot_CR/.venv-local/bin/python
# BOT_SERVICE=robotics-otp          # systemd unit name for the live bot
# BOT_PM2_NAME=robotics-otp         # ...or the pm2 process name (use one or the other)

# ── plugin half ──
# MC_JAR=/home/swirx/dev/projects/mc-link/build/libs/MCLink-0.1.0-SNAPSHOT.jar
# MC_PLUGINS_DIR=/var/opt/paper/plugins   # Paper server plugins dir (run on that host)
# MC_RELOAD_CMD=                         # e.g. "rcmdr reload confirm" (optional)